<%= form_for [@phase.lead , @phase, Comment.new] do |form| %>

  <div class="form-group">
    <%= form.text_area :body, class: "form-control", placeholder: "Add a comment" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn_primary"%>
  </div>
<% end %>
